# teraterm-login
telnet / ssh compatible

```bash
PROTOCOL = SorT ; Tn:Telnet, S1:SSHv1, S2:SSHv2
HOSTNAME = hostname
HOSTADDR = ipaddress
USERNAME = user
PASSWORD = passwd
ENABLEPW = enpasswd
```
